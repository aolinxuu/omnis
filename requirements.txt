# Detection. Off-the-shelf weights only - no training in this build.
ultralytics>=8.3.0

# Frame decode and hue scoring. On DGX Spark / ARM64 prefer the headless wheel;
# the GUI build drags in X11 deps that are not present in the container image:
#   pip install opencv-python-headless
opencv-python>=4.10.0

# VSS REST client.
requests>=2.32.0
